# Fichier d'exemple pour les variables d'environnement
# Copier ce fichier en .env et remplir avec vos vraies valeurs

# SÉCURITÉ - IMPORTANT: Changer ces valeurs en production !
# Générer une clé avec: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=votre-cle-secrete-super-longue-et-aleatoire-ici
ADMIN_USERNAME=admin
ADMIN_PASSWORD=votre-mot-de-passe-admin-securise

# Base de données
# En développement: SQLite (par défaut)
# En production: PostgreSQL (FORTEMENT RECOMMANDÉ)
# Exemples:
#   PostgreSQL: postgresql://user:password@localhost:5432/dbname
#   Heroku:     postgres://... (sera automatiquement converti en postgresql://)
DATABASE_URL=sqlite:///instance/app.db

# Configuration Flask
FLASK_ENV=production  # production ou development
FLASK_DEBUG=False     # TOUJOURS False en production
PORT=5000

# Dossier d'upload
UPLOAD_FOLDER=static/uploads

# Configuration Email (optionnel mais recommandé pour reset password, etc.)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=votre-email@gmail.com
MAIL_PASSWORD=votre-mot-de-passe-application-gmail
MAIL_DEFAULT_SENDER=votre-email@gmail.com
